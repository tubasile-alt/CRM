<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<title>Prescrição Médica</title>

<style>
/* RESET TOTAL */
* { margin:0; padding:0; box-sizing:border-box; }

@page { size: A4; margin: 0; }

html, body {
  width: 210mm;
  height: 297mm;
  background: #fff;
  font-family: "Times New Roman", serif;
  color: #000;
}

/* FOLHA */
.sheet {
  width: 210mm;
  height: 297mm;
  padding: 35mm 25mm 30mm 25mm;
  position: relative;
}

/* TÍTULO */
.title {
  text-align: center;
  font-size: 22px;
  font-weight: bold;
  letter-spacing: .5px;
  margin-bottom: 18px;
}

/* PACIENTE */
.patient {
  text-align: center;
  font-size: 14px;
  margin-bottom: 12px;
}

.line {
  border-top: 1px solid #000;
  margin: 14px 0 18px;
}

/* SEÇÕES */
.section {
  margin-top: 18px;
}

.section-title {
  font-weight: bold;
  font-size: 15px;
  margin-bottom: 10px;
}

.med {
  margin-bottom: 10px;
}

.med-name {
  font-weight: bold;
}

.med-instr {
  margin-left: 0;
  font-size: 13px;
}

/* RODAPÉ */
.footer {
  position: absolute;
  bottom: 25mm;
  left: 25mm;
  right: 25mm;
  display: flex;
  justify-content: space-between;
  align-items: flex-end;
}

.date {
  font-size: 13px;
}
</style>
</head>

<body onload="window.print()">
<div class="sheet">

  <div class="title">PRESCRIÇÃO MÉDICA</div>

  <div class="patient">
    Paciente: <strong>{{ patient.name }}</strong>
  </div>

  <div class="line"></div>

  {% if prescription.medications_oral %}
  <div class="section">
    <div class="section-title">USO ORAL</div>
    {% for med in prescription.medications_oral %}
      <div class="med">
        <div class="med-name">{{ med.medication }}</div>
        <div class="med-instr">{{ med.instructions }}</div>
      </div>
    {% endfor %}
  </div>
  {% endif %}

  {% if prescription.medications_topical %}
  <div class="section">
    <div class="section-title">USO TÓPICO</div>
    {% for med in prescription.medications_topical %}
      <div class="med">
        <div class="med-name">{{ med.medication }}</div>
        <div class="med-instr">{{ med.instructions }}</div>
      </div>
    {% endfor %}
  </div>
  {% endif %}

  <div class="footer">
    <div>
      <img src="{{ url_for('static', filename='img/carimbo.png') }}" style="width:220px">
    </div>
    <div class="date">
      Data: {{ prescription.created_at.strftime('%d/%m/%Y') }}
    </div>
  </div>

</div>
</body>
</html>
