<!doctype html>
<html lang="pt-br">
<head>
<meta charset="utf-8">

<style>
:root{
  --SAFE_TOP: 55mm;
  --SAFE_SIDE: 22mm;
  --SAFE_BOTTOM: 28mm;
}

@page { size: A4; margin: 0; }

body{
  margin:0;
  font-family: Arial, Helvetica, sans-serif;
  color:#111;
}

.page{
  padding: var(--SAFE_TOP) var(--SAFE_SIDE) var(--SAFE_BOTTOM);
}

/* TÍTULO */
.title{
  text-align:center;
  font-size:17px;
  font-weight:700;
  margin-bottom:8mm;
}

/* PACIENTE */
.patient{
  font-size:13px;
  margin-bottom:6mm;
  border-bottom:1px solid #000;
  padding-bottom:3mm;
}

/* SEÇÕES */
.section{
  margin-top:6mm;
}

.section-title{
  font-weight:700;
  font-size:13px;
  margin-bottom:3mm;
}

.med{
  margin-bottom:5mm;
}

.med-name{
  font-weight:700;
  font-size:13px;
}

.med-inst{
  font-size:12.5px;
  line-height:1.4;
}

/* RODAPÉ MÉDICO */
.footer{
  position: fixed;
  left: var(--SAFE_SIDE);
  right: var(--SAFE_SIDE);
  bottom: 18mm;
  font-size:11.5px;
}

.signature-line{
  border-top:1px solid #000;
  width:260px;
  margin-bottom:4mm;
}

.footer-content{
  display:flex;
  justify-content:space-between;
  align-items:flex-end;
}

.footer-left{
  line-height:1.3;
}

.footer-right{
  text-align:right;
}
</style>
</head>

<body>
<div class="page">

<div class="title">Prescrição Médica</div>

<div class="patient">
<strong>Paciente:</strong> {{ patient.name }}
</div>

{% if prescription.medications_oral %}
<div class="section">
<div class="section-title">Uso Oral</div>
{% for med in prescription.medications_oral %}
  <div class="med">
    <div class="med-name">{{ med.medication }}</div>
    <div class="med-inst">{{ med.instructions }}</div>
  </div>
{% endfor %}
</div>
{% endif %}

{% if prescription.medications_topical %}
<div class="section">
<div class="section-title">Uso Tópico</div>
{% for med in prescription.medications_topical %}
  <div class="med">
    <div class="med-name">{{ med.medication }}</div>
    <div class="med-inst">{{ med.instructions }}</div>
  </div>
{% endfor %}
</div>
{% endif %}

<div class="footer">
  <div class="signature-line"></div>

  <div class="footer-content">
    <div class="footer-left">
      <strong>Dr. Arthur Basile</strong><br>
      CRM 125.217<br>
      Dermatologista | CRM 125.217
    </div>

    <div class="footer-right">
      Data: {{ prescription.created_at.strftime('%d/%m/%Y') }}
    </div>
  </div>

  <div style="margin-top:4mm;">
    Especialista em Dermatologia SBD | Av. Prof. João Fiúsa, 2300, Ribeirão Preto, SP<br>
    www.clinicabasile.com.br | Instagram: @clinicabasilemed
  </div>
</div>

</div>
</body>
</html>